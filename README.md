# Continuous Integration

This `engine3d-dev/ci` repository contains a collection of GitHub Actions workflows that are designed to automate the process for building, testing, and deployment of engine3d libraries. Workflows is designed to be used with the engine3d library, but can be adapted in the use of other libraries, included.
